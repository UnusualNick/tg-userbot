services:
  userbot:
    build: .
    container_name: topic-silencer
    restart: unless-stopped
    volumes:
      - ./.env:/app/.env
      - ./data:/app/data
    # Keep TTY open for logs and potential interaction
    tty: true
    stdin_open: true
